---
milestone: v1.0
audited: 2026-03-01T13:00:00Z
status: tech_debt
scores:
  requirements: 9/9
  phases: 5/5
  integration: 5/5
  flows: 5/5
gaps: []
tech_debt:
  - phase: 01-foundation
    items:
      - "undo.flush_undo_history() exported but never called by any production module (inline undojoin pattern used in edit.lua instead). Dead export — not a bug, but API surface advertises a capability no code exercises."
      - "util.pos2byte, util.char_at, util.byte_len, util.char_len, util.display_width — all exported and tested but have zero callers in production code. Pre-built foundation utilities awaiting Phase 5+ consumers."
  - phase: 02-session-lifecycle
    items:
      - "Full keymap table (all VM_* keys) deferred to Phase 6 by design — only 'v' key installed. Documented in SUMMARY."
      - "guicursor modification (cursor shape change on mode switch) deferred to Phase 6."
  - phase: 04-normal-mode-operations
    items:
      - "edit.lua exports 10 fully implemented functions (exec, yank, paste, dot, g_increment, change, case_toggle/upper/lower, replace_char) but none are wired to keymaps. No user action can invoke them interactively. Phase 6 keymap wiring is the explicit next step."
      - "config.get() not called by any production module — config values are stored by setup() but never consumed. Options like filesize_limit, case_setting, highlight.cursor are inert until Phase 5+."
      - "Comment in edit.lua lines 221-225: 'Phase 5 will handle insert-mode entry' from _exec_change. Intentional scope boundary."
  - phase: 04.1-gap-closure
    items:
      - "VM_ highlight groups only defined when user calls require('visual-multi').setup(). The plugin/visual-multi.vim shim calls require('visual-multi') but NOT setup(). Groups are undefined if a user loads the plugin with config=false (e.g. lazy.nvim lazy load without setup call). Correct lazy-nvim pattern — regression spec confirms groups resolve after setup(). Worth documenting in README."
---

# v1.0 Milestone Audit Report

**Milestone:** v1.0 — vim-visual-multi Lua Rewrite
**Audited:** 2026-03-01
**Status:** TECH_DEBT (no blockers — all requirements satisfied)
**Auditor:** Claude (gsd-audit-milestone)

---

## Executive Summary

All 9 requirements assigned to completed phases (1–4.1) are satisfied. All 5 phase verifications passed. All 5 cross-phase integration flows are complete and wired. The milestone audit finds **no critical blockers** and **no unsatisfied requirements**.

This is the **second audit** of v1.0. The first audit (2026-03-01T01:00:00Z) found `gaps_found` with FEAT-06, FEAT-07, and FEAT-10 partial. Phase 4.1 (gap closure) resolved all three. This audit re-runs with all gaps closed and confirms clean status.

Accumulated tech debt items are all documented scope boundaries between completed phases and planned phases 5–8 — not shortcuts in completed work.

---

## Requirements Coverage (9/9)

### 3-Source Cross-Reference

| Requirement | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|-------------|-----------------|---------------------|-----------------|--------------|
| LUA-01 | Phase 1: passed | 01-01-SUMMARY | `[x]` Complete | **satisfied** |
| LUA-02 | Phase 1: passed | 01-01, 01-02, 01-04-SUMMARY | `[x]` Complete | **satisfied** |
| LUA-03 | Phase 1: passed | 01-02-SUMMARY | `[x]` Complete | **satisfied** |
| CFG-01 | Phase 2: passed | 02-01-SUMMARY | `[x]` Complete | **satisfied** |
| FEAT-03 | Phase 2: passed | 02-01-SUMMARY | `[x]` Complete | **satisfied** |
| FEAT-07 | Phase 3+4.1: passed | 03-02, 04.1-01-SUMMARY | `[x]` Complete | **satisfied** |
| FEAT-05 | Phase 4: passed | 04-01, 04-02-SUMMARY | `[x]` Complete | **satisfied** |
| FEAT-06 | Phase 4+4.1: passed | 04-01, 04-02, 04.1-01-SUMMARY | `[x]` Complete | **satisfied** |
| FEAT-10 | Phase 4+4.1: passed | 04-03, 04.1-01-SUMMARY | `[x]` Complete | **satisfied** |

**Score: 9/9 satisfied**

**Orphan detection:** No orphaned requirements. All 9 REQ-IDs assigned to phases 1–4.1 appear in at least one VERIFICATION.md.

**Out-of-scope requirements** (assigned to phases 5–8, not yet started): CFG-02…CFG-10, FEAT-01, FEAT-02, FEAT-04, FEAT-08, FEAT-09, FEAT-11, FEAT-12, FEAT-13, FEAT-14, FEAT-15, TEST-01, TEST-02. These are excluded from this milestone audit scope.

---

## Phase Verification Summary (5/5)

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 01-foundation | ✓ passed | 11/11 | 39 tests; no BUG-02/03/04; no line2byte |
| 02-session-lifecycle | ✓ passed | 8/8 | 63 cumulative tests; VMEnter/VMLeave; reentrancy guard |
| 03-region-and-highlight | ✓ passed | 13/13 | 73 cumulative tests; 4 VM_ groups; dual-extmark extend regions |
| 04-normal-mode-operations | ✓ passed | 10/10 | 100 cumulative tests; exec/yank/paste/dot/g_increment/case/replace |
| 04.1-gap-closure | ✓ passed | 4/4 | 102 cumulative tests; GAP-01 + GAP-02 closed |

---

## Cross-Phase Integration (5/5 flows)

| Flow | Status | Path |
|------|--------|------|
| Plugin load → setup() → define_groups() → VM_ groups ready | ✓ Complete | `plugin/visual-multi.vim` → `init.M.setup()` → `highlight.define_groups()` |
| session.start() → region.new() + hl.redraw() → visible cursor | ✓ Complete | `session.start()` → `region.new()` extmark → `hl.redraw()` clear+draw |
| edit.exec(dw) → undo.begin_block + undojoin + end_block → single undo | ✓ Complete | `edit.exec()` → `undo.begin_block()` → loop with undojoin → `undo.end_block()` |
| session.stop() → highlight.clear() → options restored + VMLeave | ✓ Complete | `session.stop()` → `highlight.clear()` nvim_buf_clear_namespace → `_restore_options()` → VMLeave |
| g_increment × N cursors → undojoin between iterations → 1 undo entry | ✓ Complete | `edit.g_increment()` → `first/undojoin` pattern → `undo.begin/end_block()` |

---

## Gap Closure History

### First Audit (2026-03-01T01:00:00Z) — gaps_found

| Gap ID | Requirement | Issue | Resolution Phase |
|--------|-------------|-------|-----------------|
| MISSING-01 (GAP-01) | FEAT-07 | `highlight.define_groups()` never called from `M.setup()` — VM_ groups undefined at runtime | Phase 4.1 |
| MISSING-02 (GAP-02) | FEAT-06, FEAT-10 | `g_increment` produced N undo entries for N cursors — no undojoin between cursor iterations | Phase 4.1 |

### This Audit (2026-03-01T13:00:00Z) — tech_debt

Both gaps resolved by Phase 4.1 (commits `2f19077`, `b528528`). No new gaps found.

---

## Tech Debt Inventory

No critical blockers. All items below are **documented scope boundaries** or **dead code** — not implementation shortcuts.

### Phase 01-foundation

| Item | Severity | Impact |
|------|----------|--------|
| `undo.flush_undo_history()` exported but not called in production; `edit.lua` uses inline undojoin instead | Low | Dead export; semantically equivalent implementation exists |
| `util.pos2byte`, `util.char_at`, `util.byte_len`, `util.char_len`, `util.display_width` — tested but no production callers | Low | Pre-built utilities for Phase 5+; LUA-03 invariant holds by absence of line2byte |

### Phase 02-session-lifecycle

| Item | Severity | Impact |
|------|----------|--------|
| Full VM_* keymap table deferred to Phase 6 — only `v` key wired | Low | Plugin not fully interactive until Phase 6; documented deferral |
| `guicursor` shape change on mode switch deferred to Phase 6 | Low | Documented in SUMMARY |

### Phase 04-normal-mode-operations

| Item | Severity | Impact |
|------|----------|--------|
| `edit.lua` 10 functions fully implemented but not wired to any keymap — unreachable interactively | Medium | Required to complete FEAT-05/10 for end users; Phase 6 is the explicit resolution |
| `config.get()` never called by any production module — config stored but not consumed | Medium | User's `setup(opts)` values have no behavioral effect on Phase 4 code paths until Phase 5+ |
| Phase 5 scope comment in `edit.lua` lines 221-225 (`_exec_change`) | Low | Intentional; not a stub |

### Phase 04.1-gap-closure

| Item | Severity | Impact |
|------|----------|--------|
| VM_ groups only defined when user explicitly calls `setup()` — shim does not auto-call it | Low | Correct lazy-nvim pattern; regression spec validates the path; should be documented in README |

---

## Test Suite

**Final state: 102 tests, 0 failures**

| Spec file | Tests | Notes |
|-----------|-------|-------|
| config_spec.lua | 7 | Full config lifecycle |
| edit_spec.lua | 28 | +2 GAP regression specs (04.1) |
| highlight_spec.lua | 15 | +1 GAP-01 regression spec (04.1) |
| region_spec.lua | 9 | Extend-mode, sel_mark_id |
| session_spec.lua | 24 | VMEnter/VMLeave, reentrancy |
| undo_spec.lua | 7 | BUG-02/03/04 regression guards |
| util_spec.lua | 12 | All 7 utility functions |

---

## Verdict

**Status: TECH_DEBT** (→ `tech_debt`)

- **9/9** requirements satisfied
- **5/5** phase verifications passed
- **5/5** integration flows complete
- **0** critical blockers
- **0** unsatisfied requirements
- **0** orphaned requirements
- Accumulated tech debt: 8 deferred items, all low–medium severity, all explicitly scoped to phases 5–8

---

*Audited: 2026-03-01*
*Auditor: Claude (gsd-audit-milestone)*
*Previous audit: 2026-03-01T01:00:00Z (gaps_found → resolved by Phase 4.1)*
