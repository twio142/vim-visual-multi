---
phase: 04.1-gap-closure
plan: "01"
subsystem: highlight, edit
tags: [lua, neovim, highlight-groups, undo-grouping, undojoin, regression-tests]

# Dependency graph
requires:
  - phase: 04-normal-mode-operations
    provides: g_increment, M.exec, undo.begin_block/end_block, edit.lua structure
  - phase: 03-region-and-highlight
    provides: highlight.define_groups(), VM_ highlight group definitions
  - phase: 01-foundation
    provides: M.setup(), init.lua entry point, config.apply()
provides:
  - M.setup() now calls highlight.define_groups() ensuring VM_ groups registered at plugin load
  - g_increment loop uses undojoin between cursor iterations for single undo entry
  - GAP-01 regression spec in highlight_spec.lua
  - GAP-02 regression spec in edit_spec.lua
affects:
  - phase-05-insert-mode (inherits corrected undo grouping semantics)
  - phase-06-keymap-wiring (inherits correctly defined highlight groups at setup time)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "M.setup() wires both config.apply() and highlight.define_groups() — plugin init pattern"
    - "undojoin guarded by `if not first` boolean — mirrors M.exec pattern for all multi-cursor loops"
    - "GAP regression specs: nvim_get_hl(0, {name=...}) returns {} for unknown groups, non-empty for defined"

key-files:
  created:
    - test/spec/highlight_spec.lua (extended with setup() regression test)
    - test/spec/edit_spec.lua (extended with g_increment multi-cursor regression test)
  modified:
    - lua/visual-multi/init.lua
    - lua/visual-multi/edit.lua
    - test/spec/highlight_spec.lua
    - test/spec/edit_spec.lua

key-decisions:
  - "GAP-01 fix: define_groups() added as second line in M.setup() after config.apply() — ensures colorscheme overrides still win (default=true)"
  - "GAP-02 fix: undojoin pattern mirrors M.exec exactly — first boolean tracks first cursor iteration, undojoin called before feedkeys on iterations 2+"
  - "GAP-02 regression spec uses undolevels=-1 then buf_set_lines then restore pattern for clean undo baseline (established in Phase 04-02)"

patterns-established:
  - "All multi-cursor loops that call feedkeys must use the first/undojoin pattern to satisfy FEAT-06"
  - "define_groups() must always be called from M.setup() — never assumed to be pre-called by highlight module load"

requirements-completed: [FEAT-07, FEAT-06, FEAT-10]

# Metrics
duration: 2min
completed: 2026-03-01
---

# Phase 4.1 Plan 01: Gap Closure Summary

**VM_ highlight groups wired into M.setup() (GAP-01) and g_increment undojoin added for single-undo-entry guarantee across N cursors (GAP-02); 102 tests passing, 0 failures**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-01T12:13:23Z
- **Completed:** 2026-03-01T12:14:48Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- GAP-01 closed: `require('visual-multi').setup()` now calls `highlight.define_groups()`, making VM_Cursor/VM_CursorSecondary/VM_Extend/VM_ExtendSecondary/VM_Insert/VM_Search groups available immediately after plugin load
- GAP-02 closed: `g_increment` loop now uses `if not first` / `undojoin` pattern (identical to `M.exec`), ensuring N cursors produce exactly 1 undo entry for the sequential increment operation
- 2 regression specs added (102 total, up from 100); both new specs pass confirming the fixes are correct

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire define_groups() into M.setup() and add undojoin to g_increment** - `2f19077` (fix)
2. **Task 2: Add regression specs for both gaps** - `b528528` (test)
3. **Task 3: Run full test suite and verify all tests pass** - (verification only, no new files)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `lua/visual-multi/init.lua` - Added `require('visual-multi.highlight').define_groups()` as second line in M.setup()
- `lua/visual-multi/edit.lua` - Added `local first = true` before g_increment loop; added undojoin block guarded by `if not first`; added `first = false` after feedkeys
- `test/spec/highlight_spec.lua` - Added `T_hl['setup() defines VM_ highlight groups']` spec using nvim_get_hl to confirm VM_Cursor and VM_Extend resolve after setup()
- `test/spec/edit_spec.lua` - Added `T['g_increment multi-cursor produces exactly one undo entry (GAP-02 regression)']` spec with explicit 3-cursor file-backed session

## Decisions Made

- GAP-01 fix placed as second line in M.setup() (after config.apply()) so that config is applied first, then groups registered — order matches expected plugin initialization flow
- GAP-02 used identical pattern to M.exec to keep codebase consistent: any future multi-cursor loop that calls feedkeys should follow the same first/undojoin idiom
- GAP-02 regression spec uses `undolevels=-1` + `buf_set_lines` baseline flush pattern (established in Phase 04-02) for correct undo seq_cur baseline

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - both fixes were simple 1-2 line changes as the audit predicted. All 102 tests passed on first run.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- FEAT-07, FEAT-06, FEAT-10 requirements satisfied; v1.0 milestone audit gaps fully closed
- Phase 5 (insert mode) inherits correct undo grouping semantics
- Phase 6 (keymap wiring) inherits correctly defined highlight groups at setup time
- No open blockers from this plan

## Self-Check: PASSED

- FOUND: lua/visual-multi/init.lua
- FOUND: lua/visual-multi/edit.lua
- FOUND: test/spec/highlight_spec.lua
- FOUND: test/spec/edit_spec.lua
- FOUND: .planning/phases/04.1-gap-closure/04.1-01-SUMMARY.md
- FOUND: commit 2f19077 (fix: GAP-01 and GAP-02 source fixes)
- FOUND: commit b528528 (test: regression specs)

---
*Phase: 04.1-gap-closure*
*Completed: 2026-03-01*
